# Feature Backlog

> Managed with Orbital, dogfooded on its own repo. This file is **tracked in git** —
> it's the shared, reviewable roadmap. Orbital's runtime state lives in the
> gitignored `orbital-data/`, not here.

## Ground rules for Orbital

- **Read freely.** You may read any file in this repo to understand the codebase.
- **Write directly only here.** `BACKLOG.md` is yours to update without asking.
- **Production code = propose, don't push.** Any change under `agent_os/`, `web/`,
  `scripts/`, `installer/`, etc. must be surfaced as a diff for human approval first.
  Never auto-commit.
- These are *conventions*, not a sandbox. The enforced control is the project's
  **`supervised`** autonomy preset (approve every tool call) + git diff review.

## Now (in progress)

| # | Feature | Notes |
|---|---------|-------|
| — | *(empty — items 18/19/20/21 verified merged to main and shipped in v0.7.6; moved to Done 2026-07-21)* | |

## Next (queued)

| # | Feature | Notes |
|---|---------|-------|
| 55 | **BUG FIX: stray Windows console window flashes during agent run (icacls visible)** | 2026-08-12 · **spec-written** · [spec](BACKLOG/specs/055-windows-stray-console-window.md) · User report (translated from Chinese): *"有个问题，目前在Windows上，偶现agent运行过程中出现命令行窗口"* — screenshot at `uploads/2026-08-12T141236-27b89f33c766b0c3b37f1a9a9e3bd00b.png` shows a **black `conhost.exe` window with title `C:\Windows\SYSTEM32\icacls.`** overlaying the chat during a routine read-the-readme task. **Root cause (verified by claude-code via static analysis, NOT yet on a real Windows machine):** the icacls flash is one visible instance of a **systemic gap** — 9 subprocess-creation sites across 6 files (`agent_os/platform/windows/permissions.py:153` `_run_icacls` is the one in the screenshot; plus `grep_tool.py:299`, `workspace_scan.py:30`, `shell.py:214`, `setup_engine.py:234,334,355`, `migration.py:237`, `settings.py:503`) never pass `creationflags=CREATE_NO_WINDOW`. Intermittency ("偶现") explained — `WindowsPlatformProvider.grant_folder_access` fires once per `start_agent` call (every fresh handle / post-idle-eviction / post-daemon-restart), NOT once per message. PyInstaller `console=False` is correct and unrelated; bootloader does not re-attach a discarded console. **Recommended fix:** **hybrid (A+B)** — a tiny cross-platform-safe helper (e.g. `win_no_window_flags()` returning `subprocess.CREATE_NO_WINDOW if sys.platform == "win32" else 0`) at a location importable from every affected module without a Windows-only import edge (NOT under `agent_os/platform/windows/` — that module can only be imported on Windows, but 8 of the 9 sites are cross-platform files that also run on macOS); apply inline at all 9 sites + extend to 3 transport sites that already use `CREATE_NEW_PROCESS_GROUP` (`pty_transport.py:66`, `acp_transport.py:135`, `cli_adapter.py:149`) by OR-ing the new flag in; investigate `process.py:411` (`launch_with_pty` / ConPTY) on real Windows first — design may already suppress window. `shell=True` present at 4 sites (`setup_engine.py:234,334,355`, `settings.py:503`); add the window-suppression flag but leave `shell=True` removal for a separate hardening item. No code path intentionally relies on a visible console (every subprocess is a background daemon operation — ACL grants, ripgrep searches, CLI health checks, sub-agent I/O). Effort **S–M** (mechanical diff, but invisible to existing CI — must be hand-verified on a CI-built `.exe` per the cardinal release rule, same caveat as #36 and #54). **Seven user decisions pending in spec §6:** (1) scope per-call vs helper vs hybrid → hybrid (recommended); (2) include the 3 `CREATE_NEW_PROCESS_GROUP`-only transport sites → same fix (recommended); (3) `process.py:411` ConPTY → verify on Windows first (recommended); (4) `shell=True` at the 4 sites → add window-suppression, leave `shell=True` removal separate (recommended); (5) source-text regression test (recommended) + manual on-Windows Task Manager checklist; (6) test scope → all sites, single cheap test (recommended); (7) release urgency — bundle with next regular release vs out-of-band patch.
| 53 | **BUG FIX: queue composer doesn't support media input** | 2026-08-11 · **spec-written** · [spec](BACKLOG/specs/053-queue-media-input.md) · User report: the Queue tab's composer can't attach media (images/files), unlike the main chat composer. **Root cause (verified by claude-code): frontend-only gap** — the backend already carries queue-item attachments end-to-end (`ItemRecord.file_refs` in `queue/models.py:70`, accepted by `POST /queue/items`, persisted, and expanded into the same `<attached_files>` block at dispatch by `dispatcher.py:756-777` via `format_prefix_from_refs`, regression-tested). `QueueComposer.tsx` simply has no attachment UI and `useQueue.ts:66-79` never sends `file_refs`. Upload lifetime is fine (files land in `<workspace>/uploads/`, no TTL/sweeper). **Recommended fix:** (a) extract the chat's ~130-line attachment state machine into a shared `useAttachments` hook, consume in both composers; also render filenames on `QueueItemCard`. **Key trap:** the queue must send bare content + `file_refs` (NOT the client-built `<attached_files>` block like chat does) or the block appears twice. Effort **M** (S if scoped to picker-only + no card change). All 7 §6 decisions locked to defaults 2026-08-11. **2026-08-11 · IMPLEMENTED (uncommitted)** — extraction succeeded: shared `useAttachments` hook (ChatView −190 lines, no behavior change), QueueComposer picker+paste+chips (`projectId` now required), `useQueue.addItem` sends `file_refs`, `QueueItemCard` renders filenames; wire format confirmed bare-content+refs (test asserts no client-built `<attached_files>` block); 17 new/updated tests; gates green; live daemon POST/GET smoke passed. Follow-up filed in spec §6.6: dispatch-time existence check. Pending review + QR mobile upload test over relay. |
| 54 | **BUG FIX: Windows shows multiple Orbital icons in the system tray** | 2026-08-11 · **spec-written** (static analysis — needs on-Windows discrimination checks, spec §2.8) · [spec](BACKLOG/specs/054-windows-duplicate-tray-icons.md) · **Two confirmed-from-code causes:** (1) **no single-instance guard on the desktop shell** — `is_already_running()` (`main.py:849-853`) only skips starting a second *daemon*; a second `Orbital.exe` launch always falls through to `start_tray()` (`main.py:877-885`, sole creation site `tray.py:38-57`) → one live icon per process. Everyday repro: window-close hides to tray (`main.py:570-578`) → relaunch from shortcut; installer has no `AppMutex` and auto-launches post-install (`agentos-setup.iss:52-53`). (2) **quit path never removes the icon** — `on_quit` posts async `WM_STOP` then `shutdown()` runs `os._exit(0)` before pystray's loop can send `NIM_DELETE` (verified in installed `pystray/_win32.py`) → ghost icons that linger until hovered (Win-behavior hypothesis). PyInstaller/multiprocessing re-exec **ruled out**; dead-code `request_elevation` re-exec flagged as a latent hazard. **Recommended fix (one commit):** named kernel mutex + surface-existing-window via `FindWindowW`, `AppMutex=` in the .iss, graceful tray teardown (hide before stop, exit off the callback thread), fail-OPEN if mutex held but no window found. Rewrites `test_is_already_running_path_starts_tray` to the new invariant. User ran the §2.8 checks 2026-08-11: icons **survive hover + working menus + each opens a window** → live duplicate instances (cause #1 confirmed primary; "1 Task Manager entry" likely the grouped Processes view — confirm via Details tab). **2026-08-11 · IMPLEMENTED (uncommitted)** — session-local mutex `OrbitalDesktopShell` + surface-existing-window (fail-OPEN), `AppMutex` in the .iss (name parity asserted by test), graceful tray teardown (hide before stop, exit off callback thread), unrecognized-arg guard (allows `-psn_*`); guard sits AFTER the 3 recognized flags (installer runs `--setup-sandbox` while an instance may be live — deviation recorded); old regression test rewritten + 22 new behavioral tests (fake ctypes/pystray); unit suite 3089 green. **Real verification pending: CI-built `.exe` smoke on Windows** (5-step script in the spec; upgrade-over-running now prompts — release-notes item). |
| 49 | **BUG FIX: provider settings page shows too much inline hint text — should be collapsed or hover-only** | 2026-08-11 · **spec-written** · [spec](BACKLOG/specs/049-provider-page-hint-text-overflow.md) · User report (translated): *"provider界面下有很多提示语，这里需要隐藏，或者做一个hover提示之类的，不能就这样展开"* · **Root cause (verified by claude-code):** `web/src/components/LLMProviderSettings.tsx` (no `settings/` subdir — components live flat under `web/src/components/`) emits ~10 i18n-driven hint `<p>` blocks against `web/src/i18n/strings.ts` (`llm.*` keys around `:405–455`) as unconditional siblings of form fields; the file's one existing `showAdvanced` toggle hides advanced settings but not hints. Hint blocks at `:708-710` (provider notes, raw string), `:711-713` (noChinaEndpoint), `:761-763` (getKey+howTo), `:777-793` (current-key masked), etc. **Recommended fix:** shape (a) collapse-into-`<details>` for the i18n-driven hints, leave the `.notes` raw hint unconditional (varies per provider). **Decisions locked 2026-08-11:** user accepted all spec §6 recommendations (collapse long hint paragraphs into native `<details>` closed by default; keep the short China-region warnings always visible; `.notes` translation gap = separate follow-up). **2026-08-11 · IMPLEMENTED (uncommitted)** — `HintDetails` native-`<details>` wrapper on the 5 long hint blocks, warnings stay visible; 1 i18n key (`llm.hints.details` en+zh); 5 new tests; gates green. Pending review + QR check (note: "Get your API key ↗" link now sits behind the collapse — split it out if it reads wrong on device). |
| 50 | **BUG FIX: fallback-model form has no recommended models for the selected vendor** | 2026-08-11 · **spec-written** · [spec](BACKLOG/specs/050-fallback-model-no-recommendations.md) · User report (translated): *"备用模型设置模型的时候，没有推荐使用的模型"* · **Root cause (verified by claude-code):** the backend catalog already exists — `ProviderInfo.suggested_models: string[]` is populated from `agents/config/providers.json` → `GET /api/v2/providers` → frontend `ProviderInfo.type`. The primary provider's "模型" field consumes it; the **fallback-model form simply fails to read it and falls back to a plain `<input>` with generic placeholder**. Pure wiring fix, no backend change needed. **Recommended fix:** shape (a) HTML `<datalist>` bound to `selectedVendorInfo.suggested_models` while keeping the free-text input for paste-old-model. **Decisions locked 2026-08-11:** `<datalist>` (a); **static catalog only** — user: the live model fetch "doesn't really work most of the time"; duplicate the ~10 lines (no shared hook); native filtering; add the small vitest. **2026-08-11 · IMPLEMENTED (uncommitted)** — `<datalist>` (id via `useId()` to survive dual-mount) bound to `suggested_models`, static-only, free text preserved; 4 new tests; gates green. Pending review + QR check. |
| 51 | **BUG FIX: sidebar avatars missing or non-distinct for Orbital / Claude Code / Codex / other sub-agents** | 2026-08-11 · **spec-written** · [spec](BACKLOG/specs/051-sidebar-agent-avatars-missing.md) · User report (translated): *"orbital和claude code，codex都没有对应头像"* · **Root cause (verified by claude-code):** the sidebar's agent-row component renders an `<Avatar>` primitive with no `src` and a generic gray fallback for every sub-agent; the agent data carries an `id`/`slug` but no `icon_url` field, and no `web/public/agents/` asset directory exists yet. Note: the bug report's "no Orbital avatar too" is **partially inaccurate** — Orbital's own row shows its "O" mark, but it falls through the same shared gray placeholder once it loses its title-bar special-case. **Recommended fix:** shape (a) ship per-agent SVG assets at `web/public/agents/{slug}.svg` + thread `src` through the avatar component, with first-letter-of-name fallback for unknown/MCP agents. **Decisions 2026-08-11:** user deems the trademark concern trivial — use **real vendor logos** (phase 2 promoted into scope; verified the installed claude/codex packages ship no logo assets — source from `@lobehub/icons`/simple-icons or official brand pages instead); management avatar = **Orbital's own logo**. User waived location confirmation 2026-08-11 — implemented against the confirmed surface (`agentIcons.ts`/`MessageAvatar.tsx`). **2026-08-11 · IMPLEMENTED (uncommitted)** — exact-slug `ICONS` registry (+cursor/grok), real vendor SVGs vendored from lobehub/lobe-icons (MIT; license text ships in `web/public/agents/README.md`; `currentColor` pinned to literal brand colors — `<img>`-loaded SVGs can't see page CSS), management avatar = Orbital's `/icon-192.png` via the same lookup (`◐` branch deleted), `onError` → monogram fallback; 2 stale ◐ tests rewritten; browser-verified at 26px; gates green. Pending review + QR check. |
| 52 | **BUG FIX: pause button icon is hollow/outlined — users mistake it for a checkbox/checkmark** | 2026-08-11 · **spec-written** · [spec](BACKLOG/specs/052-pause-button-hollow-icon.md) · User report (translated): *"暂停按钮是空心的，让用户误会是勾选"* · **Root cause (verified by claude-code):** `lucide-react` has separate `Pause` (`ChatView.tsx`, 14px) and `Square` (`ChatView.tsx:3526`, 18px, per-turn cancel) — both rendered with the library's global `defaultAttributes` (`fill: "none"`, `stroke: "currentColor"`, `strokeWidth: 2`). At 14px, the two thin stroked rectangles collapse visually into shapes ambiguous enough to read as a checkbox or checkmark. **Note:** the task brief's recommended shape (a) `PauseCircle` filled is **not literally available** — lucide has no separate filled variant, and `PauseCircle` is a multi-path outer-circle + inner-bars composite that risks muddy renders if naively filled. **Recommended fix:** shape (b) keep `Pause` and pass `fill="currentColor"` (each `<rect>` fills cleanly because no fill override is set; sibling `Play` / `Square` should get the same treatment for consistency). **Decisions locked 2026-08-11:** simple fill it is — `fill="currentColor"` on `Pause` + sibling `Play` icons, keep stroke defaults, `Square` cancel out of scope, skip vitest, live visual check required. **2026-08-11 · IMPLEMENTED (uncommitted)** — `fill="currentColor"` on `Pause` + both `Play` usages in `QueueHeader.tsx`; before/after browser check confirms the fix. Pending review + QR check. |
| 47 | **TokenDance provider + one-click login key provisioning (PKCE loopback)** | **2026-08-11 · TIER 2 UNBLOCKED — partner shipped R1–R4, verified against their live docs** (tokendance.space/docs/api-key-oauth; content extracted from the SPA bundle chunk `protocol-quickstart-*.js` + the `APIKeyAuthorization-*.js` authorize-page component). **Matches spec variant 1:** authorize page `https://tokendance.space/auth?callback_url=&code_challenge=&code_challenge_method=S256&app_url=&key_name=` with loopback `http://127.0.0.1:{any port}` explicitly supported; codes one-time + 10-min expiry, wrong verifier does NOT burn the code; exchange `POST /portal/api/v1/auth/keys` `{code, code_verifier, code_challenge_method}` → `{"key": …}`, no client secret, key never in a URL; consent screen shows app name + user-confirmed quota/period/expiry/RPM caps (R4 via user-set limits). **Two implementation deltas from spec §3d:** (1) no first-class `state` param — their sanctioned mechanism is embedding an opaque nonce in `callback_url`'s query (docs explicitly preserve existing query params and recommend exactly this); verify it on callback as before; deny path appends `error=access_denied`. (2) exchange body adds `code_challenge_method`. **Bonus:** headless copy-the-code mode (no callback_url, S256 mandatory) for SSH/remote. PKCE is optional for callback-mode callers on their side but Orbital always sends S256, so our codes stay verifier-bound. `code_verifier` charset RFC 7636 (43–128 of `A-Za-z0-9-._~`). CTO guidance: pass our site URL as `app_url` (= same value as the Tier-1 `X-Site-URL` header — their docs require the two dimensions to carry the same URL). **Still open (non-blocking):** R6 signup-grant-through-this-flow confirmation, R7 machine-readable pricing (CNY placeholder still pending), Q1 region/phone-scope. No staging env — CTO says test with own account on prod (R8 waived). **2026-08-11 · TIER 2 IMPLEMENTED + LIVE-VERIFIED (uncommitted, same session as the unblock):** new `agent_os/providers_auth/tokendance.py` (loopback + state-nonce-in-callback_url + always-S256 + JSON exchange + balance validation before accept; key never logged/in URLs/in responses), `POST /api/v2/providers/tokendance/signin` (persists via credential store, returns set-flag + mask only), one-click button in Global Settings (en+zh, waiting/success/error states, global-mode only). 11 py tests (all failure modes persist nothing; `app_url`↔`X-Site-URL` parity pinned) + 5 vitest; full suites green (3100 py / 1047 vitest / i18n clean). **Live end-to-end run against production succeeded with the user authorizing in the browser: real key minted, balance-validated, landed in keychain (`sk-c...0298`, source: keychain).** Remaining: R6 grant confirmation / R7 pricing JSON / Q1 region (partner side, non-blocking); wizard-surface integration + ZH-overflow screenshot pass at ship time. | · 2026-08-07 · **spec-written, design agreed** · [spec](BACKLOG/specs/047-tokendance-provider-oneclick-key.md) · TokenDance (词元跳动, tokendance.space; Watcha/观猹-incubated "Chinese OpenRouter") as a **parallel** provider (not featured): registry entry + locale-keyed ordering with a 仅中国大陆 note, a 「一键登录，免费领取 Token」 button, and OpenRouter-style **PKCE loopback key provisioning** (variant 1: one-time code + exchange; system browser, never embedded webview; key validated via their free balance endpoint then stored through the existing keychain path). Rides the production PKCE machinery in `agent_os/connectors/oauth.py` (Google constants generalized). **Tier 1** (registry + region + button-as-link + `X-App-Name`/`X-Site-URL` attribution headers, ~1.5–2 d) ships independently; **Tier 2** (provisioning flow, ~3–4 d) blocked on the partner building authorize-page + code-exchange endpoints (their side is small — Watcha already runs the OAuth server). Full partner requirements R1–R8 + Q1–Q4 in spec §4, ready-to-send Chinese version in spec Appendix A. Key risks: `phone` scope may bound registration to mainland China (Q1); their streaming failover can't engage mid-stream on the Anthropic-protocol path. |
| 46 | **Minimal telemetry: aggregate-only daily ping** | **2026-08-08 · IMPLEMENTED — client on `feature/backlog-46-telemetry` (`380c9e7`+`29e0ed5`), server pushed to `zqiren/orbital-telemetry` (`411c25b`)** — full spec §10 scope: version injection (fixes stale-egg-info misreport), identity/spool/rollup/sender, all six emit seams (turn_completed rides `ledger.append_event` — single chokepoint covering management + both sub-agent transports), settings toggle + verbatim payload viewer + wizard disclosure (en+zh), docs/TELEMETRY.md published schema, README sections. §12 decisions locked: Q2 keep local error spool when off · Q3 13mo→monthly · Q4 startup+6h. 26 new unit tests + 4 vitest + 7 server tests; all gates green; isolated live-daemon smoke verified end-to-end vs a local ingest stub (fresh-install detect, exact schema, live toggle-kill both directions). **2026-08-08 · DEPLOYED:** Railway service `orbital-telemetry` live in the `agentos-relay` project (Postgres attached, auto-deploy from repo main) at `https://orbital-telemetry-production.up.railway.app` — exactly the client's baked-in default, no client change needed; production contract verified (401 no-token / 200 valid+idempotent resend / 422 malformed; synthetic row `inst_000000000000` left in `pings` — filter or delete). **Pending:** Q1 disclosure-copy sign-off; live-LLM `turn_completed` smoke (release pre-flight); review/merge of `feature/backlog-46-telemetry`. · 2026-08-07 · **spec-written, design agreed** · [spec](BACKLOG/specs/046-minimal-telemetry.md) · Measure users / retention / error rate / funnel / token consumption without violating local-first: raw events stay in a local spool (`{data_dir}/telemetry/events.jsonl`), only a daily aggregate JSON (counters + enums + lifetime milestone booleans, no content/paths/IDs) is sent to a new `POST /ingest` + Postgres on the existing Railway deployment. Random resettable `install_id` in the data dir (NOT relay `device.json`); consent = default-on with first-run disclosure + Global Settings toggle + verbatim payload viewer; nullable `account_id` from day one for the future account system. Includes two prerequisites with independent value: runtime **version injection** (nothing knows its own version today — stale egg-info returns 0.7.2 on a 0.8.4 install) and **structured `llm_error` persistence** (`classify_llm_error` codes currently die in memory, `agent_manager.py:3522`). Verified greenfield: shipped app makes zero calls to Orbital infra (relay env-gated off, `app.py:466-467`). Client S–M (~3–5 d), server ~1–2 d (relay repo). Open questions in spec §12: disclosure copy, error-spool behavior when toggled off, server retention, cadence. |
| 48 | **PERF: session switching lag — especially when switching while a session is starting/running** | **2026-08-11 · spec-written** · **Summary:** Root cause is a single synchronous `list_sessions()` call on the FastAPI event loop (`agents_v2.py:2361`) that hits every session switch. Every switch fires 4 uncached REST round trips, and while a session is running, the same blocking scan fires *repeatedly* on every `agent.status` WS event, contending with the running session's own blocking writes. Spec: [`BACKLOG/specs/048-session-switching-lag.md`](BACKLOG/specs/048-session-switching-lag.md) · **2026-08-11:** user asked whether this means sessions need lazy-loading at scale — answered no: chat history is already paginated; the fix is threading the scan off the event loop (A) + mtime-keyed per-file cache (B), which makes steady-state cost O(changed files). **2026-08-11 · IMPLEMENTED (full A+B+C+D bundle, uncommitted working tree)** — backend scans threaded + mtime-keyed `_disk_entry_cache` (skip-verdicts cached, live `scope` excluded, defensive copies; 6 unit tests); frontend per-session history cache (stale-while-revalidate, no skeleton on switch-back) + AbortControllers on history/holder/sub-agent fetches (also fixes an out-of-order holder overwrite) + memoized sidebar rows/sort; 2 new ChatView tests. All gates green; live daemon smoke passed. Pending human review + commit. |
| 45 | **BUG FIX: clicking delete on a workbench item creates a duplicate (only disappears after refresh)** | **2026-08-08 · IMPLEMENTED on `fix/china-launch-batch` (`136e404`)** — root cause found: id-less flagged entries (no `<!--mem-->` comment) parse to `id=null`, collapse onto one React key, and the delete POSTs to `/entries/null/exit` → 404 silently reverted. Fix heals id-less entries. Verified in-tree + branch CI green (run 31199308430); moves to Done when the branch merges. · 2026-08-07 · **pending-spec** · User report (translated from Chinese): *"有个问题，目前点击workbench里面item的delete的时候，会新生成一个一样的item，只有刷新之后才会消失"* — pressing the delete control on a workbench item spawns a duplicate row instead of removing the original; the duplicate only clears after a full page refresh. **No investigation yet.** Plausible shapes to check (none pre-decided): frontend optimistic insert not removed on 2xx, WebSocket `workbench_item_added` double-firing alongside `workbench_item_removed`, list cache not invalidated by the delete handler, backend delete returning 200 but emitting an `add` event, or a v0.8.x race where the click handler hits both an "archive" and a "create draft" branch. Needs dispatch to claude-code to confirm root cause, map the affected code paths, and write the implementation spec under `BACKLOG/specs/045-…md`. **Pending user confirmation before dispatch** (per backlog rules). |
| 44 | **BUG FIX: session chat history pinned to bottom — user cannot scroll up** | **2026-08-08 · IMPLEMENTED on `fix/china-launch-batch` (`f28a8af`)** — `useAutoScroll.ts` auto-follow arbiter + jump-to-latest pill landed. Verified in-tree + branch CI green (run 31199308430); moves to Done when the branch merges. · 2026-08-06 · **spec-written** · [spec](BACKLOG/specs/044-session-chat-scroll-lock.md) · User report: *"现在有一个问题，orbital在跑的时候，session的聊天记录一直pin在底部，用户不能向上滚动"* — during an active Orbital run the chat transcript stays locked to the bottom, so the user can't read earlier messages. **Root cause (verified by claude-code):** `web/src/components/ChatView.tsx` has **zero** "is the user at the bottom" detection anywhere (`grep -n 'onScroll\|isAtBottom\|isNearBottom' web/src` → 0 hits) and `scrollToBottom()` (`:831-838`) is called unconditionally from **17 call sites** — most severely the per-token `handleStreamDelta` branches at `:1523` and `:1539`, which fire on every reasoning/answer token during a stream and force `scrollTop = scrollHeight` on the next animation frame, reverting any manual scroll before the user can read it. CSS layer ruled out (the `ChatTab → ChatView → scrollRef` flex chain has correct `min-h-0` throughout). **Fix:** add new `web/src/hooks/useAutoScroll.ts` (~55 lines) with `isAtBottomRef` + scroll-position threshold (80px) + programmatic-scroll guard (150ms) + `newContentCount` counter + `scrollToBottom({ force?: true })` API; replace `scrollToBottom` def in `ChatView.tsx`, attach `onScroll={handleScroll}` to the container, reset on session switch, force-scroll on the 3 `handleSend` outcomes, and render a Slack-style "↓ N new" pill when `newContentCount > 0`. Gating lives inside `scrollToBottom()` itself, so 13 of the 17 call sites need **zero** code changes. i18n via `chat.newMessages` en+zh in `strings.ts`. **Eight user decisions pending in spec §9** (full text in spec): (1) "at bottom" threshold — 80px (recommended) or another value?; (2) pill visibility — only when `newContentCount > 0` (recommended, Slack/Discord-style) or always visible when `!isAtBottom`?; (3) pill click behavior — jump + resume auto-follow (recommended) or jump without resuming?; (4) counter granularity — count turn/tool/card-level events only (recommended, ~9 extra call sites) vs. raw deltas (would say "↓ 47 new" mid-answer, looks broken) vs. static "↓ Jump to latest" no count?; (5) scope — ChatView only (recommended, matches report; `QueueTab`/`SubAgentDrillIn` don't reproduce this bug because they never auto-scroll at all) or extend to them now?; (6) effort ≈ **S** (4-6h incl. tests) — confirm?; (7) test plan — hook unit tests with stubbed `scrollHeight`/`clientHeight`, `ChatView.test.tsx` integration block using the existing `emitWs` helper, plus the CLAUDE.md mandatory daemon-restart + QR-code smoke; (8) i18n — confirm zh-CN key with `{n}` interp. |
| 41 | **43 undocumented pre-existing test failures in `tests/regression/` + `tests/integration/`** | 2026-08-03 · Found during the 35–38 batch verification: running `pytest tests/regression/ tests/integration/ -q` (keyring env set) fails 43 tests **identically on clean main `0404d4d` and on the batch branch** (diff of sorted failure lists is empty — decisively pre-existing, not batch damage). CLAUDE.md documents only the 3 platform env-fails; these suites aren't in the standard gate at all. Includes e.g. `test_chat_reasoning_seam::test_reasoning_only_no_answer_persists_reasoning`, `test_cold_start_scan::test_empty_workspace_has_no_scan_flag`, `test_context_md_lifecycle::test_session_end_restructures_old_context_md` + 40 in regression. Needs triage: stale fixtures vs real rot vs env-dependent; then either fix or document as known-red in CLAUDE.md so future batches have a trustworthy baseline. Failure lists preserved in the 35–38 session scratchpad. S (triage). |
| 40 | **Cost-loading flash + lagging numbers in BudgetCorner/QueueTab (follow-up from #36)** | **2026-08-08 · FIXED on `fix/backlog-40-cost-freshness` (`7649308`) — pending human review, merge, and daemon/QR smoke** · Scope grew on user report: besides the first-fetch flash, the corner/queue numbers **lagged** the settings meter — a pricing-table save recomputed only `BudgetSection` (the `costRefreshKey` prop chain stopped there), and spend events missed while the WS was down left long-lived consumers stale until remount. Fix at the source in `useCost`: `loading` honest from the first render; `cost` cleared synchronously (adjust-state-during-render) on any project/window/rates change so a stale number never renders, while same-query refetches keep the last known spend; the prop-drilled `costRefreshKey` replaced by a module-level pricing-version store (`web/src/budget/pricingVersion.ts`, `useSyncExternalStore`) observed by **every** mounted `useCost`; refetch on WS reconnect. 14 hook tests incl. flash/stale-bleed/recompute/reconnect; full vitest 994 green; `tsc -b` clean. · Original row (2026-08-03): #36 fixed the settings page's false "$0.00" / "no sub-agents" states only (its decision 3); the other `useCost` consumers still painted a fabricated zero during the first fetch. XS → landed as S with the lag scope. |
| 39 | **Cache-aware pricing: add `cached_input_per_1m` for DeepSeek/MiniMax so the budget ledger stops billing cache hits at the flat input rate** | **2026-08-08 · DONE (edit in working tree on `fix/china-launch-batch`, pending commit)** — `cached_input_per_1m` added to every DeepSeek entry (chat 0.028 / reasoner 0.14 / v4-flash 0.0028 / v4-pro 0.003625 / `_default` 0.028) and every MiniMax entry (0.06 across M3/M2.7/M2.5/`_default`) in `agent_os/config/providers.json`. Rates: official DeepSeek pricing docs (api-docs.deepseek.com) + MiniMax M3 launch pricing ($0.06/1M cache read at the promo tier matching the $0.3/$1.2 already in the table). The ledger/editor machinery already priced `cache_read` separately (`ledger.py:425`, PricingEditor "Cached input" column) — this was data only. 243 pricing/budget unit tests green. · 2026-08-03 · Follow-up from #38 §5 fix 3 (decision Q3: separate item). `providers.json` carries no cached rate for DeepSeek/MiniMax, so `budget_spent_usd` is cache-blind — and once #38 ships and the hit rate jumps ~23%→~90%, the overbilling becomes material (hits bill ~10× cheaper than the flat rate). S. |
| 34 | **Project onboarding: import existing sessions/projects from CLI agents (codex, claude code, etc.) into Orbital** | **2026-08-08 · IMPLEMENTED on `fix/china-launch-batch` (`613e84b`)** — scan-and-suggest on first run per the locked verdict (never auto-create, link-only, no transcript transcoding); Claude Code + Codex + Obsidian sources. Verified in-tree + branch CI green (run 31199308430); moves to Done when the branch merges. · 2026-08-02 · **spec-written** · [spec](BACKLOG/specs/034-import-external-agent-sessions.md) · User ask (memmy agent inspiration): *"memmy agent will retrieve from all cli agent such as codex and claude code the session memory and migrate it to memmy. can we do a similar onboarding that will scan all the session or projects of the existing tools and set up orbital projects?"* Dispatched to claude-code; spec written and verified at `BACKLOG/specs/034-import-external-agent-sessions.md` (912 lines, 10 sections + verdict). **Verdict: (d) — scan-and-suggest, never auto-create; link-first, transcode never.** Ship a read-only **discovery** scanner over `~/.claude/projects/`, `~/.codex/sessions/` (+ its `state_5.sqlite` index) and friends that produces a **ranked, deduplicated, user-confirmed candidate list**, then routes each confirmed candidate through the **existing** `POST /api/v2/projects` + **existing** cold-start scan. **Do not write foreign transcripts into `orbital/sessions/*.jsonl`** — four cited code paths would silently adopt them as Orbital's own history. Session *history* enters as a distilled read-on-demand digest (phase 2), never as replayable session rows. **Effort: S for the MVP (~90% of value), M with the P2 digest.** Everything expensive in this idea (project bootstrap from an existing folder) **already exists and shipped** — item #34 is the missing *discovery* half. **memmy agent confirmed real**: [MemTensor/memmy-agent](https://github.com/MemTensor/memmy-agent), MIT, TS, 510 stars, two weeks old; its target is a memory store, not a project list — scanner half maps onto Orbital, destination half does not. **Measured on this machine (live disk data, not docs)**: 139 candidate projects, 112 pointing at non-existent paths → empirical anti-auto-create argument. **In-scope agents for v1: Claude Code (479 sessions / 412 MB) + Codex (214 / 203 MB)** — only verified sources with authoritative `cwd` per session. Cursor empty; Gemini `chats/` empty; Copilot `ide/` empty; Aider/OpenCode/OpenClaw/Hermes/WorkBuddy not installed → cannot verify. **Five user decisions in spec §9** (Q1 link-only vs copy vs hybrid · Q2 auto-create vs always-confirm · Q3 redact/warn/refuse on credentials · Q4 which agents in v1 scope · Q5 ship before/after Spec 001 cloud hosting). Open questions Q6–Q12 in spec §7. **2026-08-02:** coordinator recommendation sheet for all twelve questions recorded in spec §11 (link-first P1 · always-confirm · CC+Codex only · before Spec 001 · P3 out of scope · disclose-then-scan · basename naming). **Deferred from the current weekly release** — needs a dedicated verification + test pass before shipping (reads other apps' data, creates projects). |

## Next (queued)

| # | Feature | Notes |
|---|---------|-------|
| 33 | **Investigate rowboat's connector system — can Orbital reuse it?** | **PAUSED 2026-08-02** (user: connector work on hold) · 2026-07-30 · **spec-written** · [spec](BACKLOG/specs/033-rowboat-connector-investigation.md) · User ask (translated): *"让Claude code看一下这个项目，重点看一下connector是怎么实现的， orbital能不能复用"* (https://github.com/rowboatlabs/rowboat). Dispatched to claude-code; full repo cloned + read end-to-end. **Verdict: (e) Not a fit** — rowboat is a **competing local-first desktop agent product** (Electron, YC S24, 16.9k stars, daily releases), not a connector library. Its "connector system" is **~500 LoC of REST client in front of Composio + a stock MCP client** — no MCP server in the tree, no per-provider code, no CLI/daemon surface, no per-toolkit `server_url`. Apache-2.0 license clean; zero China coverage (worse than open-connector). **All four reuse shapes collapse**: (a) Tier-0 MCP source — impossible, zero `@modelcontextprotocol/sdk/server` imports repo-wide; (b) manifest import — `CURATED_TOOLKITS` is 67 × `{slug, displayName, category}`, less than Composio's own `/toolkits` and lacks the fields manifests require; (c) standalone sidecar — rowboat's "native API" is Electron IPC, the only loopback seam (`127.0.0.1:3210/_rowboat/*`) is gated by a spoofable `X-Rowboat-App: 1` header with no token; (d) hybrid — every component is one of the above. **Independent confirmation of Spec 14 §G**: rowboat shipped `composio-google-migration.ts` to *remove* Composio Gmail/Calendar/Drive and replace them with native OAuth, and their agent prompt now says *"Native connections win over Composio"* — a funded team with 13 contributors tried full-broker and walked the sensitive core back off it. **Orbital's substrate beats rowboat's on every axis** (spec §5.1 table): richer manifest schema (carries the fields rowboat fetches at runtime), tokens in OS keychain (rowboat uses plaintext JSON + Composio cloud), per-project enablement (rowboat has none), first-class namespaced tools (rowboat uses meta-tool gateway for *every* connector, paying Spec 14 §Risks UX-degradation cost everywhere). **Carry-forward:** one ~20-line design note for `agent_os/connectors/manifest.py` — borrow rowboat's `discovery: {issuer|static}` + `client: {static|dcr}` auth-provider split (validated by their DCR-based `fireflies-ai` provider); S, deferred until a real non-Google OAuth connector demands it. **Six open questions for the user in spec §9**: (1) accept the (e) verdict and close #33 without follow-up? (2) does rowboat's production reversal settle the Composio question for Spec 14 §G / §7-Q1′? (3) promote the manifest-schema note to its own backlog item, or leave it here? (4) log rowboat as **competitive intelligence** (different item scope from connectors)? (5) investigate rowboat's `apps/x/packages/core/src/{code-mode,runtime/acp,runtime/transports}` as a second ACP-shaped sub-agent reference for Spec 16 / #32? (6) does zero-CN-coverage from two independent broker probes raise Feishu's priority in Spec 14 §E launch order? XS investigation effort. |
| 32 | **Survey: external agent SDKs / CLIs that could plug into Orbital as sub-agents (workbuddy, kimi-code, pi, openclaw, hermes, + Tencent CodeBuddy)** | **PAUSED 2026-08-02** (user: SDK-integration work on hold) · 2026-07-29 · **spec-written** · [spec](BACKLOG/specs/032-external-agent-sdk-survey.md) · User ask (translated): *"research what agent SDKs can be integrated, like pi, kimi-code, etc."* — later named: **workbuddy · kimi code · pi · openclaw · hermes**. Research done directly by feature-log (browser.search + browser.fetch) per user preference — no sub-agent dispatched. Spec at [`BACKLOG/specs/032-external-agent-sdk-survey.md`](BACKLOG/specs/032-external-agent-sdk-survey.md) covers all 5 user-named + bonus Tencent CodeBuddy + brief landscape sweep (Aider/Goose/Cline/Continue/OpenHands/Plandex/Codex-CLI/Claude-Code-CLI — all ruled out). **Ranked findings:** (1) **Pi (Inflection)** — ✅ recommend-now, real first-party TS SDK `@earendil-works/pi-coding-agent`, Anthropic-API-compatible, clean drop-in to existing SDK adapter seam. (2) **Kimi Code (Moonshot)** — ✅ recommend-now, prefer **ACP path** via `kimi acp` (cheapest, reuses Cursor/Grok adapter); SDK `@moonshot-ai/kimi-agent-sdk` also exists. (3) **Hermes (Nous Research)** — 🟡 recommend-later, cheapest to wire (ACP server + `hermes-acp-sdk` PyPI listed on agentclientprotocol.com), but positioned as personal-gateway not coding-agent — needs a one-day spike first. (4) **Tencent CodeBuddy** — 🟡 recommend-later bonus, both ACP and SDK (`codebuddy-agent-sdk` PyPI), user didn't name but worth flagging. (5) **OpenClaw** — ❌ skip for "as sub-agent" use case (it's a peer platform, not an SDK; reverse-direction MCP integration would be a different kind of item). (6) **WorkBuddy (`workbuddy.ai`)** — ❌ skip, NOT a coding agent — likely a name confusion with Tencent CodeBuddy (flagged in spec as naming watchlist item b). **Adapter-seam neutrality preserved**: every recommend-now / recommend-later candidate uses one of Orbital's three existing adapter shapes (SDK / ACP / Appserver) — no item-24 neutrality violation, no CLI-pipe adapters introduced. **Two naming-confusion watchlist items** flagged in spec: (a) `kimi-cli` vs `kimi-agent-sdk` — two different repos, two different integration shapes, future items must pick deliberately; (b) `WorkBuddy` (research-agent product) vs `CodeBuddy` (Tencent coding-agent) — different companies. Suggested follow-up items 033–036 listed at the end of the spec for the user to decide whether to create. S. |
| 31 | **BUG FIX: sub-agents never see `retractions.md` — the user's "no" does not propagate to the agents most likely to violate it** | **PAUSED 2026-08-02 — pending user decision on Delete/veto semantics (see end of row)** · 2026-07-27 · **diagnosed, unfixed** · Found while staging demo footage. The retraction store exists so a user's explicit "no" survives across agents, but it reaches **only management-agent sessions**. `context.py:283-290` injects `retractions.render_constraints(...)` as an unconditional hard-constraint system block every turn — verified live: M3 refused a tempting re-add and cited the retraction, its date, and its reason. But `sub_agent_prompt.py` never references retractions at all: it names `PROJECT_STATE.md, DECISIONS.md, LESSONS.md, INDEX.md` (`:161`) plus the agent's own `MEMORY.md`, and nothing else. So a dispatched Claude Code / Codex / Cursor worker — precisely the agent most likely to *write user-facing deliverables* (a site, a report, a resume) — has never been told the user vetoed a claim. **Second, independent defect:** the mechanical backstop is far weaker than its docstring implies. `flag_chokepoint` re-add matching is a 0.75 normalized-difflib threshold, which catches near-verbatim only. Measured against a retracted title: verbatim 1.000 blocked; same sentence with the tail trimmed 0.935 blocked; **"The project increased revenue by 40%." 0.690 → ALLOWED**; "boosted revenue by ~40%" 0.565 → allowed; "Revenue grew roughly 40 percent thanks to the project." 0.196 → allowed. Confirmed end-to-end: a rephrased re-add lands on disk with a fresh id and **no warning**. So the only thing actually covering rephrases is the injected instruction ("do not re-propose, re-infer, or re-add any of them under any wording") — which sub-agents don't get. Net: the propagation guarantee the feature is named for is the one case that isn't enforced. Fix shape: render the retraction constraint block into `render_sub_agent_prompt` (it inlines nothing today by design — this would be the first exception, or pass the path and instruct a read), plus a producer↔consumer test asserting every context builder that can write project state sees the retraction block. S. **2026-08-02 verification + user challenge:** defect confirmed still present (zero `retraction` references in `sub_agent_prompt.py`), but the premise needs a decision first. Facts: `retractions.md` is NOT a Layer-1 file (Layer-1 = PROJECT_STATE/DECISIONS/LESSONS/INDEX, `workspace_files.py:161-168`); the Workbench exit that writes it was relabeled **"Delete"** in spec rev 4 (`371e682`) yet still sends `kind='irrelevant'` → `add_retraction` (`workbench.py:318-321`) — so pressing Delete silently records a permanent veto. User questions whether Delete should carry veto semantics at all. Options: **(1)** Delete means delete — retractions written only by an explicit "never suggest again" action; #31 shrinks accordingly; **(2)** keep Delete-as-veto — then implement #31 as specced; **(3)** drop the retraction store — close #31 won't-fix. Do not implement until answered. |
| 22 | **Reactive sub-agent supervision and recovery contract** | 2026-07-16 · Investigation confirms the core loop already exists: dispatch yields; completion/error/failure/interruption/user-stop/background-loss wakes the owning management session; the manager can verify, fix directly, follow up, or restart fresh under normal autonomy. Proposed scope is **XS, 0.5–1 day**: add a precise negative-outcome recovery ladder to the management prompt, lock user-stop/autonomy behavior in tests, and sharpen README positioning. No watchdog, progress classifier, retry engine, worker scoring, or new UI. Alignment requested on user-stop as a universal hard boundary, Check-in's existing automatic re-dispatch semantics, and prompt-only vs persistent retry guard. Spec at [`BACKLOG/specs/022-…md`](BACKLOG/specs/022-reactive-sub-agent-supervision.md). |
| 16 | **Cursor + Grok as sub-agents via an official-ACP-SDK transport** | **PAUSED 2026-08-02** (user: SDK-integration work on hold; provider/account gates also unresolved) · 2026-07-16 · **Cursor-only v1 implementation complete on `codex/cursor-grok-acp-implementation`; standalone Grok deferred.** **2026-07-21 status check: branch is MERGED to main (`9950d28`) and shipped in the v0.7.6 build — live daemon reports Cursor installed + ready (`/api/v2/agents/available`). Kept in Next (not Done) because the live provider/account gates below are still unresolved.** Added pinned official `agent-client-protocol==0.11.0`, a typed streaming `ACPSDKTransport`, Cursor manifest/detection/auth/model settings, user-visible `auto` (default) / `ask` permission policy through Orbital's existing card, provider-neutral blocked-interaction wake + `agent_message.respond`, provider-confirmed resume reporting, per-handle prompt FIFO, process-tree teardown, and English + Simplified Chinese UI. Independent review corrections prevent persistent provider grants, aggregate ACP text deltas into one answer, preserve the ten-minute bypass and permission option IDs, reject dead-stream queues honestly, and keep task text separate from workspace instructions/private memory. Automated Python/TypeScript/Vitest/i18n gates and isolated real-daemon Cursor+Grok protocol smoke pass. **Live limitations:** the authenticated account returns “Upgrade your plan to continue,” so Grok-generated content and Cursor question/plan extension names remain unverified; Cursor `session/load` returns ACP `Invalid params` for a just-created session, and the transport fails visibly rather than falsely claiming resume. Awaiting human diff review and resolution of those provider/account gates before production-ready status. Spec at [`BACKLOG/specs/016-…md`](BACKLOG/specs/016-cursor-grok-acp-sdk-transport.md). |
| 14 | **Use open-connector as an MCP server source for connectors** | **PAUSED 2026-08-02** (user: connector work on hold) · 2026-07-06 · Can Orbital consume `oomol-lab/open-connector` (840+ providers, 8,300+ Actions, exposes `/mcp`) as a Tier-0 MCP source under Spec 11's connector substrate? Sub-agent verdict: **consumable today** via the existing Custom MCP server form at `ConnectorSettings.tsx` + `POST /api/v2/connectors/custom` (`agent_os/api/routes/connectors.py:64-72`) — zero Orbital code changes. But `/mcp` exposes only 4 meta-tools (`list_apps`, `search_actions`, `get_action_guide`, `execute_action`), not direct action passthrough, so deeper integration needs an agent-loop extension. Integration shapes (a–d) compared: standalone sidecar / catalog import at build / user-installed Tier-0 / hybrid. Awaiting user answers on deployment model (local-first posture), OAuth app-registration responsibility, curated-vs-full-catalog posture. **2026-07-07 research addendum** (spec §"2026-07-07"): broker landscape + OAuth-app cost + connector roadmap. Verdicts — **Nango is not the fit** (BYO OAuth app per provider; moves tokens off keychain); **Composio** is the only managed broker that owns verified OAuth apps (removes per-service registration) but stores tokens on its SaaS cloud (breaks local-first); **first-party is best for the free/sensitive tier** since OAuth registration is ~free (real money = Google restricted-scope CASA $500–4.5k/yr + WeChat 企业认证 ¥300/$99; real barrier = a Chinese entity for CN platforms). **China workspace suite (Feishu/DingTalk/WeChat) is first-party regardless** — even China-origin open-connector ships only notification bots for them. Connector tiers + launch order recorded. Spec at [`BACKLOG/specs/014-…md`](BACKLOG/specs/014-open-connector-as-mcp-source.md). |

## Later (someday / ideas)

| # | Feature | Notes |
|---|---------|-------|
| 42 | **Cross-project residue layer (PROFILE.md + GLOBAL-LESSONS.md)** | 2026-08-04 · **proposed** · **Goal:** stop lessons and preferences from dying at project folder boundaries — the permanent-junior problem recreated at project granularity. **Rationale:** each project currently cold-starts on things already learned elsewhere (tool quirks, standing preferences, patterns that worked). **Proposed shape:** two files owned by **this** backlog project (sibling of `BACKLOG.md`): (a) **`PROFILE.md`** — thin, slow-changing user layer; standing preferences, tooling, collaborators; **propose-then-approve for all writes**; injected into **every** dispatch briefing to any project; (b) **`GLOBAL-LESSONS.md`** — terse, source-attributed, generalizable entries only (admission test: *would this matter in an unrelated project?*); relevant entries injected into dispatch briefings **by domain**. **Sequence:** #43 (closeout) writes into `GLOBAL-LESSONS.md` — this item lands first. **Not implementing yet.** **Next action:** draft both file skeletons and a briefing-injection convention for the user to review. |
| 43 | **Project closeout / distillation procedure** | 2026-08-04 · **proposed** · **Goal:** projects terminate, but their value shouldn't — define what happens when a project is marked closed. **Rationale:** without a closeout, finished projects are a graveyard of folders; the compounding-asset story caps out per-project. **Proposed shape, on closure:** (1) extract generalizable lessons into `GLOBAL-LESSONS.md` (from #42 — **blocked on #42**); (2) write a ~10-line **`POSTMORTEM.md`** into the project folder (goal, outcome, what shipped, why closed); (3) list reusable artifacts and propose promoting them to a shared `snippets/` folder; (4) update the project registry with status and postmortem pointer. **Folder is archived, never deleted.** **Not implementing yet.** **Next action:** write the closeout procedure as a checklist; trial-run it on the most recently finished project. |
| 1 | **Cloud hosting for project files** | 2026-06-26 · user-flagged "very important". Implementation spec at [`BACKLOG/specs/001-…md`](BACKLOG/specs/001-cloud-hosting-for-project-files.md); see Spec 1 note below. |
| 10 | **Group projects — shared project state + merge engine** | 2026-07-03 · Strategy note. Group projects need a **merge/reconciliation engine** (not worktrees per se — members sync via the Spec 001 backend). Merge-engine decisions resolved (three-way + recency tiebreaker, per-change latest-edit ordering, archive losers, per-file-class policy, git-optional); first consumer is fanout phase 2. Blocked behind Spec 001's open questions. Spec at [`BACKLOG/specs/010-…md`](BACKLOG/specs/010-group-projects-shared-state.md); see Spec 10 note below. |

## Done

| # | Feature | Shipped |
|---|---------|---------|
| 2 | **Clickable local paths in chat → drawer** | 2026-07-01 · shipped in claude-code session. |
| 3 | **Render HTML files in the file viewer** | 2026-07-01 · shipped in claude-code session. |
| 4 | **Suppress CLAUDE.md conflict banner** | 2026-07-01 · shipped in claude-code session. |
| 5 | **Sub-agent session context continuity across dispatch paths** | 2026-07-01 · shipped in claude-code session. |
| 9 | **Sub-agent fanout: parallel native worker sessions** | shipped — confirmed by user 2026-07-13. |
| 12 | **Cross-project scope for Quick Tasks (the global lens)** | shipped — confirmed by user 2026-07-13. |
| 7 | **Document generation — Tier A** | shipped in v0.7.3 — verified on `main` 2026-07-14. |
| 8 | **Language toggle during onboarding** | shipped in v0.7.3 — verified on `main` 2026-07-14. |
| 15 | **`AGENTS.md` at workspace root** | shipped in v0.7.3 — verified on `main` 2026-07-14. |
| 17 | **First-launch onboarding guide — Tier 1** | shipped in v0.7.3 — verified on `main` 2026-07-14. |
| 18 | **File tree scrollbar on parent click** | merged to main via `codex/backlog-18-19`, shipped in v0.7.6 — verified 2026-07-21 (`FileExplorer.tsx:249` no longer stacks static `md:block` on the tree pane). |
| 19 | **Sibling-session list/read tools for the management agent** | merged to main via `codex/backlog-18-19`, shipped in v0.7.6 — verified 2026-07-21 (`agent_os/agent/tools/sessions_tool.py` present and registered). |
| 20 | **BUG FIX: sub-agent bubbles paired to wrong transcript turn** | merged to main via `98db7c8` (`fix/subagent-turn-pairing`), shipped in v0.7.4+; pairing verified working live 2026-07-21 (one bubble per dispatch, idempotent join). **Follow-up (a) — @mention double-writes markers — still open, now tracked under item 24;** (b)/(c)/(d) remain as logged in the original row (see git history of this file for the full text). |
| 21 | **Project-level sub-agent deployment instructions** | merged to main via `cae5e29` (`codex/cursor-grok-subagents`), shipped in v0.7.6 — verified 2026-07-21 (field live in production `projects.json`). |
| 23 | **BUG FIX: sub-agent terminal errors silently swallowed (deferred-wake gap + unrendered error marker)** | merged to main via `fa886d9` — verified in-tree 2026-08-02 (`sub_agent_terminal` wake tags in `lifecycle_observer.py`, error-styled row, producer↔renderer parity fixture `subAgentMarkerFixtures.json` + `test_sub_agent_marker_parity.py`). Full diagnosis in git history of this file. Sibling wake gaps → item 28; unrendered marker shapes → item 27. |
| 24 | **BUG FIX: duplicate sub-agent header ("double codex profile") + completion-marker guidance leak** | merged to main via `71eaf1a` — verified in-tree 2026-08-02 (`_meta.display_content` split live at `lifecycle_observer.py:114`; single @mention dispatch marker). Full diagnosis in git history of this file. |
| 25 | **Simplify New Project creation** | merged to main via `1a9b3cc` — verified in-tree 2026-08-02 (create modal + inline `FolderBrowserPanel` + `POST /api/v2/platform/mkdir`; optional model/api_key). Follow-ups → item 26. |
| 29 | **BUG FIX: duplicate messages + missing intermediate thinking on queued sends** | fixed on main — D1 `3194ac7` (single user-message echo on wait-state queue + per-nonce `pending_dispatched`), D2 `400eeed` (mid-turn reseed no longer wipes the live overlay); `Bug29 D2` vitest suite in `ChatView.test.tsx`; root-cause record at [`plans/bug29-queue-dup-missing-thinking.md`](plans/bug29-queue-dup-missing-thinking.md). Verified in-tree 2026-08-02. |
| 26 | **Create-modal + folder-picker polish batch** | shipped in v0.8.2 (`4b27b2e`) — FolderPickerModal deleted (strings.ts + source CSV), mkdir absolute-parent 400, WKWebView-safe modal a11y (Escape scoped out of the embedded picker), and ALL FIVE dropped-queue sites leave durable markers (premise corrected: queue lives in `sub_agent_manager.py`, fifth site found in coordinator review). User-verified via branch-CI installer 2026-08-03. Follow-ups → item 35; full text in git history of this file. |
| 27 | **Three [Sub-agent] marker shapes render as nothing + parity fixture extension** | shipped in v0.8.2 (`7e8f617`) — four shapes covered (incl. guard-discovered `interaction_required`), exhaustive ChatView label switch with compile-time `never` proof + severity-ranked tones, AST auto-discovery parity guard replacing the hand-maintained producer list, fixtures 6→11. User-verified via branch-CI installer 2026-08-03. Follow-up → item 35; full text in git history of this file. |
| 28 | **Sub-agent lifecycle wake coverage: remaining deferred-terminal gaps** | shipped in v0.8.2 (`2e83e29`) — `completed`/`interrupted`/`fanout_join` tagged, consumer wakes on the event alone (kind list dropped), live mid-turn terminal smoke added to the CLAUDE.md release pre-flight. Mid-turn completions now hot-resume the manager by design. Full text in git history of this file. |
| 30 | **BUG FIX: AGENTS.md not seeded into new project folder on creation** | shipped in v0.8.2 (`a613ea1`) — `agent_md_seeder.py` + `create_project` wiring, never-clobber, scratch skip, committable template (no absolute paths/timestamps); live scratch-daemon smoke verified 2026-08-02. Full text in git history of this file. |
| 35 | **Dropped-queue marker shape + cross-module parity guard + i18n orphan sweep** | shipped in v0.8.3 (`4d20a10`) — amber `queue_dropped` marker with `sub_agent_terminal` wake tag (legacy `failed`-shaped rows from old builds still parse by design); parity guard now AST-scans `sub_agent_manager.py` too and caught + fixed a SIXTH unmarked drop site (prompts queued behind a raised dispatch); 4 orphaned `folderPicker.shortcut.*` keys deleted from strings.ts + CSV. Full text in git history of this file. |
| 36 | **BUG FIX: "Edit price table" blank in packaged installs + no settings loading state** | shipped in v0.8.3 (`c71e5f9`) — frozen-bundle `providers.json` path collapsed lexically (loaders warn before caching `{}`, never silent again), PricingEditor explained empty state + retry, spend meter + sub-agents section get honest loading/error states (mount-frame-safe gate). **Packaged-install verification still pending** — bug is invisible in dev builds by construction; confirm the pricing table renders on the v0.8.3 installer. Budget numbers on packaged installs will jump as real rates replace the $3/$15 fallback (noted in release notes). Follow-up → item 40; full text in git history of this file. |
| 37 | **BUG FIX: sub-agent/fanout terminal wake events invisible to DeepSeek managers** | shipped in v0.8.3 (`5303a07`) — `_meta.event`-tagged rows (`sub_agent_terminal` + `interaction_required`) remap to a positional `role:user` turn at the wire boundary, uniformly in ContextManager (zero provider-conditional logic); session JSONL/UI/wake flow untouched; 31 wire tests incl. both-adapter not-hoisted assertions + M3 byte-identical golden. **Live DeepSeek + M3 manager wake smoke still pending** (extends the #28 release pre-flight item). Full text in git history of this file. |
| 38 | **BUG FIX: DeepSeek prefix-cache ~23% — per-call `truly_dynamic` system row invalidated the conversation cache every call** | shipped in v0.8.3 (`5303a07`, bundled with #37) — runtime block folds into the final positional user turn (`[runtime]`-framed; own user row after tool-result tails) + churn trim (minute-granularity timestamp, 5%-step context display with raw-value thresholds, stable checkpoint line); offline cache regression covered by rewritten `test_cache_efficiency.py`. **Live 3-call DeepSeek cache probe still pending** (needs real key). Follow-up → item 39 (cache-aware pricing); full text in git history of this file. |

## Specs

> Detailed explorations for non-trivial backlog entries. Each spec has a status tag
> and a pointer back to its Kanban row. Status → table mapping: `idea` → Later ·
> `exploring` / `shaped` → Next · `ready` → Now · shipped → Done.

### Spec 1 — Cloud hosting for project files
**Status:** idea · **Added:** 2026-06-26

User said: *"one very important features. cloud hosting for project files."* — user-flagged "very important".

The detailed implementation spec (code paths cited, `ProjectStorageBackend` interface sketch, sync semantics per file category, sandbox + credential + offline analysis, dependencies, effort estimate, and 6 open questions for the user) lives at:
- **[`BACKLOG/specs/001-cloud-hosting-for-project-files.md`](BACKLOG/specs/001-cloud-hosting-for-project-files.md)**

→ Awaiting user answers on the 6 open questions in the spec before promoting from `idea`.

### Spec 2 — Clickable local paths in chat → drawer
**Status:** shipped · **Added:** 2026-06-26 · **Decisions resolved:** 2026-07-01 · **Shipped:** 2026-07-01

User said: *"i want the link presented by agent to be clickable - the local path. i want to invoke a side bar to visualize them"*

**Resolved decisions (spec §0):**
- Side panel → **right-hand drawer** (desktop) / **bottom-sheet** (mobile). The `files`-tab-switch alternative is not built.
- **No new agent tool** — the `MarkdownContent` link renderer is made kind-aware: workspace-relative links to previewable artifacts (`.html`, images, `.csv`, `.md`, `.json`) render as **cards** that open the drawer; source-file paths render as inline **chips**. A `present_artifact` tool is deferred until we outgrow the convention.
- Linkify **high-signal surfaces only** (markdown links, inline-code, tool-call activity rows, attachment chips) — **no plain prose** in v1.
- Read-only preview, replace-on-click, toast on missing file.
- Effort **M (~2-3 days)**, frontend-only.

Spec: **[`BACKLOG/specs/002-clickable-local-paths-in-chat.md`](BACKLOG/specs/002-clickable-local-paths-in-chat.md)**

→ Shipped 2026-07-01 in claude-code session.

### Spec 3 — Render HTML files in the file viewer
**Status:** shipped · **Added:** 2026-06-26 · **Shipped:** 2026-07-01

User said: *"i want to be able to see html in the files."*

The detailed implementation spec (file-preview code paths, current `.html` returns raw text from the file API, rendering mode options — sandboxed iframe vs raw vs code-view, asset resolution, security model, 8 open questions for the user, effort estimate) lives at:
- **[`BACKLOG/specs/003-html-rendering-in-files.md`](BACKLOG/specs/003-html-rendering-in-files.md)**

→ Shipped 2026-07-01 in claude-code session.

### Spec 4 — Suppress CLAUDE.md conflict banner
**Status:** shipped · **Added:** 2026-06-27 · **Shipped:** 2026-07-01

User said: *"there is a sign above the chat interface ... that said ... investigate into it and propose solutions on how to suppress it"*. Bug investigation only — no code change.

The detailed investigation (root cause, exact files + line numbers — `agent_os/agent/sub_agent_prompt.py:214-260` scanner, `agent_os/agent/sub_agent_manager.py:524-583` WS broadcast, `web/src/.../ClaudemdWarningBanner.tsx` rendered from `ChatView.tsx:2032`; quoted "bypass" matches at `CLAUDE.md:329` and `CLAUDE.md:492`; data flow from disk → WS → component; 5 suppression options with effort/risk/trade-offs; recommended approach) lives at:
- **[`BACKLOG/specs/004-suppress-claudemd-banner.md`](BACKLOG/specs/004-suppress-claudemd-banner.md)**

→ Shipped 2026-07-01 in claude-code session.

### Spec 5 — Sub-agent session context continuity across dispatch paths
**Status:** shipped · **Added:** 2026-06-29 · **Decisions resolved:** 2026-07-01 · **Shipped:** 2026-07-01

User said (translated from Chinese): *"In Orbital, if you first dispatch a sub-agent through the management agent, and then later call the sub-agent via `@`-syntax, the sub-agent's session is not the same — the `@`-initiated session does NOT carry forward the context from the earlier management-agent-dispatched session."*

**Resolved decisions (spec §0):**
- **Single-session model:** a sub-agent thread is one continuous thread per `(project, chat_session, handle)`; both dispatch paths into the same chat session share it. The chat session is the boundary (lifetime coupled to it).
- **Cross-session re-attach → no** (a new chat session starts sub-agents fresh). This **drops §4c and the lock re-keying** — the heaviest part of the original plan.
- **Reset via a thin flag:** `agent_message(send, …, fresh=True)` lets the management agent deliberately start a fresh sub-agent thread (unrelated task, or reset an overlong thread). Not a new tool.
- Known gap: provider-level resume is SDK/codex-only; pipe transport keeps a continuous transcript but a cold LLM thread (deferred).
- Chosen build scope **4a + 4b + 4d**, ≈ **2 days** backend, no frontend required.

Spec: **[`BACKLOG/specs/005-subagent-session-context-continuity.md`](BACKLOG/specs/005-subagent-session-context-continuity.md)**

→ Shipped 2026-07-01 in claude-code session.

### Spec 7 — Document generation support (docx / pptx / xlsx)
**Status:** ready (Tier A) · **Added:** 2026-07-03 · **Decisions resolved:** 2026-07-13

User said (translated from Chinese): *"调研一下，orbital应该怎么支持做文档，比如说docx，ppt，excel的能力"*. User added: *"顺便看一下我电脑里的workbuddy是怎么做的"*.

The detailed implementation spec (tool-layer text-only analysis with `read.py:88-89` / `write.py:49` / `edit.py:51` cited; files API classifier chain at `files_v2.py:49-192`; agent manifest survey; frontend preview pipeline at `FilePreview.tsx` + `web/src/types.ts:635`; pyproject dependency posture; three implementation tiers from XS (~50-100 LOC, skill-only) to M (~400-700 LOC, native read/write + preview) to L/XL (~1500-3000 LOC, in-app render via mammoth/SheetJS); effort estimates calibrated against shipped specs 003/005; **WorkBuddy reference** validating Tier A strongly, half-validating Tier B, fully validating Tier C's `docx-preview` + SheetJS + custom pptx-preview stack; 8 original open questions + 2 surfaced by WorkBuddy) lives at:
- **[`BACKLOG/specs/007-document-generation-support.md`](BACKLOG/specs/007-document-generation-support.md)**

→ Decisions resolved 2026-07-13 (all defaults → **Tier A**): bundle the three pure-Python libs as an `[office]` extra + doc-gen skill + prompt hint; no new tools, no UI, no pandoc/LibreOffice; read via the same libs through shell. Tier B/C only if real usage shows Tier A falling short. See spec "Resolved decisions". **Follow-up (2026-07-13, from implementation):** packaged installers don't yet carry the `office` extra — the sandboxed shell runs the *system* python, so bundling into the frozen app wouldn't serve it anyway; the prompt hint degrades gracefully (on-demand `pip install`, pypi.org is network-allowlisted). Deciding whether/how installers should pre-provision these libs is an open design question for a future pass.

### Spec 8 — Language toggle during onboarding
**Status:** ready · **Added:** 2026-07-03 · **Decisions resolved:** 2026-07-13

User said: *"we need the language toggle during onboarding."*

The detailed implementation spec (cold-start problem: English-non-speaker's first screen is English; current-state trace through `LocaleContext.tsx:18-26` → `locales.ts:5-18` → `LocaleContext.tsx:28-49` → `main.tsx:12-20` → `SetupWizard.tsx:254-361`; existing `GlobalSettings.tsx:79-93` dropdown primitive ready to drop in; `navigator.language` is currently unread; runtime is already client-side only via `localStorage['orbital.locale']`; tiered approach A1 / A2 / B1 / B2 / B3 with **A2 + B1 recommended**, A1 = S, A2 = XS, B2 = S-M on top; no new libraries, no backend changes, no `react-intl`/`i18next`/FormatJS; 8 open questions covering layout, default-locale detection, daemon persistence, language list expansion, and partial-translation fallback) lives at:
- **[`BACKLOG/specs/008-language-toggle-during-onboarding.md`](BACKLOG/specs/008-language-toggle-during-onboarding.md)**

→ Decisions resolved 2026-07-13 (all defaults): **A2 header widget** (top-right, Globe icon + select, every step) + **B1 `navigator.language` fallback**; no daemon persistence (B2 skipped); languages stay en+zh. Hard prerequisite for Spec 17 — ship first or same branch. See spec §8.

### Spec 9 — Sub-agent fanout: parallel native worker sessions
**Status:** shipped · **Added:** 2026-07-03 · **All decisions resolved:** 2026-07-04 · **Shipped:** confirmed by user 2026-07-13

User said: *"implement sub agent fanout - meaning that dispatching multiple parallel sessions for sub tasks … this is not about sub agents such as claude code and codex."*

**Resolved decisions (spec §0):**
- **Workers = native Orbital sessions** — in-process `AgentLoop` per sub-task with the project's provider + restricted tools, wrapped in the existing sub-agent adapter interface (plumbing worker-agnostic; CLI handles inherit parallelism later).
- **Initiation = management-agent tool** — one thin `fanout(tasks=[…])` batch-dispatch tool; one tool call = one `yield_turn`, so the loop's sibling-cancellation behavior needs no change.
- **Join = wait-for-all** — a `JoinGroup` in `SubAgentManager` gathers all worker completions (with timeout + partial-failure reporting) and injects **one** summary via the existing `inject_system_message` funnel.
- **Isolation (resolved 2026-07-03 in the sequencing/strategy discussion):** shared workspace + per-task file scopes with in-process write-guards in v1; worktree + **separable merge engine** as phase 2 (spec §5a addendum; merge-engine decisions recorded in Spec 10). Endorsed sequencing: fanout v1 → worktree manager + merge engine → multi-session execution.

The detailed spec (three blockers with citations — `yield_turn` single-dispatch at `loop.py:1121-1125`, first-completion-owns-restart at `lifecycle_observer.py:59-92`, one-thread-per-handle keying at `sub_agent_manager.py:77-82`; what already works — non-blocking dispatch, 5-adapter concurrency, defer-safe push-back, multi-aware dispatcher slot-hold; work packages W1–W5; Claude Code worktree-convention trade-off analysis; 8 open questions; L effort ≈ 1250–1900 LOC / 1.5–2.5 weeks) lives at:
- **[`BACKLOG/specs/009-subagent-fanout-parallel-workers.md`](BACKLOG/specs/009-subagent-fanout-parallel-workers.md)**

→ Implementation started 2026-07-04 on `feature/backlog-9-subagent-fanout` (off `feature/backlog-2-3-4-5` @ `78c05bd`). All §7 questions resolved with the user — see spec §0.5 (toolset incl. web search/fetch; cap 5; utility-model workers; activity-watchdog timeout; nested drill-in UX; approve-the-batch; fanout progress card, workers not in chips row). **Shipped — confirmed by user 2026-07-13.**

### Spec 10 — Group projects: shared project state + merge engine
**Status:** idea (strategy note) · **Added:** 2026-07-03

User said: *"we will want to support group projects in future, where all members should be able to use the same project state files and contribute back to the project."*

**Framing agreed 2026-07-03:** group projects don't require worktrees per se — members sync across machines via the **Spec 001 backend**; the strategic shared asset is a **merge/reconciliation engine** (pluggable policy) whose consumers, in order, are: fanout worker dispatch (Spec 9 phase 2) → CLI sub-agent dispatch → worktree-per-session → group-project sync reconciliation.

**Merge-engine decisions resolved:** three-way merge with last-writer-wins as the conflict *tiebreaker* only (never whole-file LWW); ordering by **per-change latest-edit time** (resolves the user's session-start-vs-latest-edit question — order changes, not sessions; git-native form = auto-commit per turn, order by commit time, buying attribution + revert); losing diffs archived + surfaced, never deleted; policy per file class (metadata may LWW/regenerate, user work product gets three-way + archive); git-optional engine (copy-dir fallback is first-class — Orbital projects are often not git repos). **Rejected:** merge-when-all-sessions-idle (idle ≠ done; long-lived session blocks all merges; breaks the shared-blackboard model).

Spec: **[`BACKLOG/specs/010-group-projects-shared-state.md`](BACKLOG/specs/010-group-projects-shared-state.md)**

→ Parked as `idea`: merge-boundary question open (turn-end auto-commit vs session close vs explicit action); transport/membership/identity blocked behind Spec 001's 6 open questions.

### Spec 11 — Connectors + the workspace layer
**Status:** shaped · **Added:** 2026-07-04 · **Design aligned:** 2026-07-04

User said: *"we need to enable connector for services such as gmail and calendar, and there are many of them, we need to have one place to host it and a way to scale the addition of these services."* Follow-ups: calendar deserves a UI (*"a place where human and agent can collaborate"*), connectors at onboarding, sandbox wizard step removed, settings need an index.

**Resolved decisions (spec §0):**
- **Substrate = MCP client in the daemon** — connectors are catalog *manifests*, not per-service code; tools reflect namespaced into the existing `ToolRegistry`; adding a Tier-1 connector ≈ a ~20-line manifest + icon + smoke test (friction model in spec §3, incl. Tier-0 user-added custom MCP servers and the Google OAuth verification caveat).
- **Auth global, enable per-project** (project = trust boundary; tokens in keychain `UserCredentialStore`); writes gated by autonomy, unknown tools fail closed.
- **Two-tier surface rule:** email = tools only; **calendar = first surface**, one component mounted as global calendar + per-project lens (external calendar stays source of truth; project linkage = Orbital metadata).
- **IA: two-zone sidebar** (Workspace zone: Calendar, promoted Quick Tasks · Projects zone unchanged); connectors get *no* sidebar surface — Global Settings section + Project Settings toggles.
- **Onboarding:** wizard → `api_key → connect your accounts` (featured-but-skippable, merged with browser sign-in); sandbox step deleted (macOS vestigial, **Windows setup moves to the installer** via the existing `--setup-sandbox` elevated path).
- **Settings index:** generalize the one-off `settingsAnchor: 'budget'` into a named-section scrollspy rail on both settings surfaces (rejected paged settings — preserves single-form-save).

Spec: **[`BACKLOG/specs/011-connectors-and-workspace-layer.md`](BACKLOG/specs/011-connectors-and-workspace-layer.md)**

→ `shaped`, phased A (connector core, L) / B (calendar surface, M-L) / C (onboarding + installer + settings index, M). Awaiting §7 answers — biggest: Google OAuth app strategy (Q1, likely decides launch set), calendar sync mechanism (Q2), remote-only vs local MCP servers (Q4).

### Spec 12 — Cross-project scope for Quick Tasks (the global lens)
**Status:** shipped · **Added:** 2026-07-04 · **Design aligned:** 2026-07-04 · **Shipped:** confirmed by user 2026-07-13

User said: *"sometimes he needs an overview of all projects or maybe even full access of the computer … projects are assets, but there are different ways to look at assets — work in progress or historical reference."* (Corporate-trainer scenario: search all client projects for prior deck style/content.)

**Resolved decisions (spec §0):**
- **Principle: every asset has two lenses** — WIP (project agent, read-write) vs reference (cross-project, read-only). Quick Tasks is the *agent* of the global lens.
- **Quick Tasks gets opt-in all-projects READ-ONLY scope** — reads resolve across all project workspaces, Write/Edit stay in scratch; agreed scope-down from the user's original "full access" phrasing.
- **Full computer access** = separate loudly-consented opt-in, deferred; **dispatch-to-project-agents** deferred to post-fanout (composes with Spec 9 machinery).
- **Prerequisite fix ships regardless:** per-project portal scoping — today `_portal_paths` is a process-global dict on the shared provider, so project A's *shell* can already read+write project B once both started (`agent_manager.py:514` + `macos/provider.py:194-198`). Real isolation leak.
- Other projects' `orbital/` runtime dirs excluded from reference reads (work product only, not agent internals).

Spec: **[`BACKLOG/specs/012-cross-project-scope-quick-tasks.md`](BACKLOG/specs/012-cross-project-scope-quick-tasks.md)**

→ Shipped — confirmed by user 2026-07-13.

### Spec 14 — Use open-connector as an MCP server source for connectors
**Status:** exploring · **Added:** 2026-07-06

User said: *"do you think we can use this for connector in orbital? https://github.com/oomol-lab/open-connector"* — pointing at `oomol-lab/open-connector`, a self-hostable MCP server (840+ providers, 8,300+ prebuilt Actions, `/mcp` endpoint).

**Sub-agent verdict (exploration complete, awaiting user shape decisions):**
- **Yes** as an opt-in Tier-0 source *today* — the existing "Custom MCP server" form at `ConnectorSettings.tsx` plus `POST /api/v2/connectors/custom` (`agent_os/api/routes/connectors.py:64-72`) already handle the integration. Zero Orbital code changes needed for users to point at their own open-connector.
- **Architectural caveat:** `/mcp` exposes only 4 meta-tools (`list_apps`, `search_actions`, `get_action_guide`, `execute_action`) — not direct action passthrough — so any deeper integration than Tier-0 requires an agent-loop extension to call `search_actions` → `get_action_guide` → execute over HTTP.
- **Not a replacement** for Spec 11's curated Google catalog: open-connector just pushes the Google OAuth app-verification problem one layer down (each user / org still needs their own registered Google OAuth client).

Four integration shapes compared in the spec (a) standalone Docker/Node sidecar / (b) catalog import at build time / (c) user-installed Tier-0 (zero-code path already works via Spec 11's existing custom-server surface) / (d) hybrid curated + import + Tier-0 fallback — with decision matrix per user persona (casual / power / enterprise). License (Apache-2.0), catalog freshness model, Cloudflare-deploy posture, and the `oo CLI` mid-July 2026 timing note all captured.

The detailed spec (Orbital-side code paths at `agent_os/connectors/manager.py:232` / `agent_os/connectors/mcp_client.py:19-29` / `agent_os/connectors/manifest.py:116-119`; open-connector catalog format + OAuth handling; four integration shapes with effort estimates; cross-cutting risks — third-party dependency, OAuth app-registration responsibility, curated-vs-full posture, license obligations on embedding; 5+ open questions for the user) lives at:
- **[`BACKLOG/specs/014-open-connector-as-mcp-source.md`](BACKLOG/specs/014-open-connector-as-mcp-source.md)**

→ Awaiting user answers on the open questions (deployment model: self-host on user machine vs Cloudflare sidecar vs OOMOL hosted; OAuth-registration responsibility; curated vs full catalog posture; Tier-0 docs guide vs first-class integration path; Apache-2.0 embedding obligations) before promoting from `exploring`.

### Spec 15 — `AGENTS.md` at workspace root (grep-discoverable onboarding for external agents)
**Status:** ready · **Added:** 2026-07-13 · **Pivot** 2026-07-13 from "sub-agent onboarding" → "external agentic-tool onboarding" · **Decisions resolved:** 2026-07-13
User said: *"got another idea. i think we should have an sub agent instruction.md so that when other agents visit our workspace. they are able to catch up with the work seamlessly"* — clarified: *"not really for sub agents. more like for other agents. for example, for codex app to open our project folder and will be able to pick up the project seamlessly. the key will be that to ensure claude code, codex app is able to identify from grep that this file is the one to read before everything. this should be simple task right?"* — i.e. **external agentic tools** (Claude Code, Codex App, Cursor, Copilot, ...) opening the project folder, with grep-discoverability as the core requirement.
[Spec at `BACKLOG/specs/015-agents-md-at-workspace-root.md`](BACKLOG/specs/015-agents-md-at-workspace-root.md) covers: 5 design axes (file name — `AGENTS.md` community convention vs `INSTRUCTION.md` vs reuse existing `CLAUDE.md`; location — workspace root, tracked in git; multiplicity — one file at root for MVP; maintenance — hand-curated vs auto-synced from `orbital/PROJECT_STATE.md`; interaction with existing `CLAUDE.md` / `ONBOARDING.md` at workspace root), an MVP content skeleton (project summary + current focus + in-progress pointers + conventions + how-to-recover-context + land-mine warnings + write-scope), the relationship to the existing `agent_os/agent/sub_agent_prompt.py` onboarding machinery (complementary, not redundant — that one only runs for Orbital-dispatched sub-agents), effort = **XS** (author the file + commit it, ~½ day), and 5 open questions for the user.
→ Decisions resolved 2026-07-13 (all defaults + content pivot): `AGENTS.md` at root, committed, hand-curated, cross-references `CLAUDE.md`. **Content = memory-system manual for external agents** (per memory file: positioning, when to read, when/how to update). **Full read/write on all memory files** — external agents are collaborators, not hostile (user); hands off only machine-managed runtime state (`orbital/sessions|ledger|tool-results` + transcript JSONLs). See spec "Resolved decisions".

### Spec 16 — Cursor + Grok as sub-agents via an official-ACP-SDK transport
**Status:** Cursor-only v1 merged to main (`9950d28`, in v0.7.6); provider/account live gates remain · **Added:** 2026-07-13 · **Implemented:** 2026-07-16 · **Merge verified:** 2026-07-21

User said: *"can you help to investigate some new cli tools such as cursor and grok. and is there a way we can integrate them as sub agents like claude code and codex?"* — then corrected the bar: *"pipe and acp transport is actually stale … please check again if there is any official ways to plug them in"*, and asked for the replacement cost + dependency assessment.

**Resolved direction (spec §Verdict):**
- **Both integrable through one seam: ACP.** No Python agent-SDK exists for either, but both ship first-party ACP server modes — Cursor `agent acp` (binary renamed `cursor-agent` → `agent`; `session/new` + `session/load` resume, `session/request_permission`, agent/plan/ask modes, MCP), Grok Build `grok agent stdio` (xAI's declared standard for external tools).
- **Build one `acp_sdk_transport.py` on the official `agent-client-protocol` PyPI SDK** (v0.11.0, official `agentclientprotocol` org, Apache-2.0), replacing homegrown `acp_transport.py` — confirmed **dead code**: no manifest routes to `transport: acp` (all ship sdk / codex-appserver / pty), so no behavior migration; ~660 test lines refactor, dummy-agent fixture reusable.
- **Cost ≈ 5–8 dev-days for the corrected v1:** ~300–500-LOC common transport + Cursor/Grok wire profiles, native approvals, provider-confirmed resume, management-agent interaction response, per-handle prompt FIFO, model/mode settings, exact-version pin, process-tree teardown, manifests, fixtures, and live verification. A richer end-user structured question/plan card is optional because the management agent can respond in v1.
- **Dependency accepted:** sole transitive dep pydantic>=2.7 (already shipped, 2.12.5), pure-Python 66KB wheel (PyInstaller-trivial, no signing implications), Python `>=3.10,<3.15` fits our `>=3.11`, actively maintained (pushed 2026-07-10); pre-1.0 risk mitigated by pinning.
- **Rejected for v1:** Cursor's official `@cursor/sdk` (TypeScript-only → Node sidecar; revisit only for Cursor cloud-VM agents). Cursor's Claude-shaped `-p --output-format stream-json` pipe path recorded as fallback only.
- **Grok's new open-source release confirms, rather than replaces, this seam.** The official repo says ACP is the embedding interface and calls stdio the primary integration mode. Its source advertises ACP v1 with `load_session(true)`, implements `session/load`, and routes permission prompts through `session/request_permission`. The source tree is a periodically synced monorepo export, not a supported Grok library API; the released `grok` executable remains the v1 runtime boundary. Source-building (`xai-grok-pager` renamed to `grok`) is useful for development/reproducibility, but bundling it would add Rust builds, Grok update ownership, and weaker Windows support for no integration benefit.
- **One remaining compatibility gate is now explicit:** Grok pins Rust `agent-client-protocol` 0.10.4 with unstable features; our proposed Python client is 0.11.0 / schema v1.16. Both negotiate ACP protocol v1 and the core payloads appear compatible, but we must run a real cross-version initialize → new → prompt → permission → load test before declaring the transport ready. Pin the Python SDK and record the tested Grok version/commit in fixtures.

Spec: **[`BACKLOG/specs/016-cursor-grok-acp-sdk-transport.md`](BACKLOG/specs/016-cursor-grok-acp-sdk-transport.md)**

→ Cursor-only v1 is implemented on `codex/cursor-grok-acp-implementation`; standalone Grok remains deferred because Cursor already exposes Grok 4.5 models. The common orchestration is provider-neutral: blocked interactions wake the management agent and are answered on the original request, task text is unchanged, permissions use Orbital policy/cards without persistent vendor grants, resume is reported only after provider confirmation, and ordinary prompts queue per handle. Real Cursor ACP initialize/new/model/prompt/end-turn/teardown and Simplified Chinese delivery are verified. Production-ready status remains gated on (a) an account that can produce real Cursor/Grok output and trigger question/plan extensions, and (b) Cursor's currently failing `session/load` behavior (`Invalid params` for a newly created session). Grok's standalone Rust ACP 0.10.4 compatibility test remains a later-profile gate.

---

### Spec 17 — First-launch onboarding guide
**Status:** ready (Tier 1) · **Added:** 2026-07-13 · **Decisions resolved:** 2026-07-13
User said: *"another idea. i think we need an onboarding guide when first launched. i saw a lot of confused user. especially on the china vs global api address and provider selection. we will also need the language toggle during the onboarding."*

**The pain points the user named** (each grounded by code citations in the spec): (F1) free-form API base URL at `LLMProviderSettings.tsx:163-166` with no region hint — users paste the wrong region; (F2) provider picker exposes only first-party IDs (Anthropic, OpenAI, Google, etc.) — no CN providers surfaced, no regional auto-suggest; (F3) cold-start screen is always English (covered by Spec 8, not re-spec'd here). The current SetupWizard is gated by `App.tsx:194-200` on `appState === 'unconfigured'`, runs two steps (`api_key` → `connect accounts`) and has no welcome / language / region step at all. `web/src/data/providers.json` defaults are global-only — no CN endpoint ever suggested.

**Three tiered approaches proposed** (each with effort + file:line citations): **Tier 0 (XS, ~1 day)** — rewrite the existing welcome copy in `SetupWizard.tsx:27-254` to explain what Orbital is in 4-6 sentences; add a region-aware hint band; surface one provider recommendation line. **Tier 1 (M, ~1 wk)** — add a `pick provider` step to the wizard with regional presets (Anthropic CN / OpenAI CN-mirror / Zhipu GLM / DeepSeek / Moonshot Kimi / Alibaba Qwen / Google Gemini / OpenRouter); suggest known CN endpoints per provider; consume [Spec 8](BACKLOG/specs/008-language-toggle-during-onboarding.md)'s language toggle (A2 widget) as a header affordance. **Tier 2 (L, ~2-3 wks)** — full multi-step guided tour: welcome → language → provider+region → API key (with explanation of what the key is) → first project → first chat; one-paragraph explanation + "learn more" link per step.

[Spec at `BACKLOG/specs/017-onboarding-guide-first-launch.md`](BACKLOG/specs/017-onboarding-guide-first-launch.md) covers: current-state citations, the three tiers with file:line diff plans and effort estimates, a provider-preset table (who offers a CN endpoint, who doesn't), the Spec 8 dependency wiring, a Tier-vs-resolves-confusion matrix, and **10 open questions** (which tier, regional preset list, official-CN-endpoint knowledge responsibility, language-toggle placement, force-once vs resumable, IA — modal vs dedicated route vs sidebar, skip-tour button + re-trigger mechanism, integration with [Spec 11](BACKLOG/specs/011-proactive-connectors-onboarding.md)'s `connect accounts` step, post-onboarding `?help` in-app guide, telemetry for where users actually get stuck).

→ Decisions resolved 2026-07-13 (3rd pass): **Tier 1 minimal** — upgraded single provider screen (Tier 2 tour deferred). Q8 promoted into scope — per-provider `console_url` + "Get your API key →" link with one-line how-to (the user's reframed priority). **Fixed preset order for everyone** (CN-friendly first; no locale logic), **default = DeepSeek, never Custom**, **region toggle defaults to China** (CN-first launch audience; region-locked keys mean the default decides who sees a first-try 401). Static "no mainland-China endpoint" captions on OpenAI/Anthropic/Google replace the locale-triggered hint band. Official CN endpoints only; Test Connection prominent but not gating; Spec 8 (A2 widget) is a hard prerequisite. See spec §9.

### Spec 18 — File tree: scroll bar doesn't appear when clicking a parent node
**Status:** shipped · **Added:** 2026-07-14 · **Decisions resolved:** 2026-07-14 · **Merged/verified:** 2026-07-21 (in v0.7.6)

User said: *"one bug here. in file tab, if you click on the parent node, the scroll bar wont occur - you have to click on the child node. this is unacceptable. please investigate into it and propose a task spec"*

**Sub-agent diagnosis:** `web/src/components/FileExplorer.tsx:247` stacks `md:block` (static) and `flex` (from the `mobileShowPreview ? 'hidden md:flex' : 'flex'` ternary) on the same tree-pane element. When `mobileShowPreview=false` — the initial state, and the state after any **parent/folder** click, since only file clicks set it to true — Tailwind's stylesheet ordering makes `md:block` win at ≥768px, breaking the flex-col chain and rendering the `flex-1 overflow-y-auto` scroll container as unbounded auto-height (no scrollbar). Clicking a **child file** flips `mobileShowPreview=true`, which adds `md:flex` to the class string and restores the flex layout (scrollbar returns). The fix is a one-line className cleanup.

The detailed spec (root-cause evidence with quoted className strings, exact `FileExplorer.tsx` line citations, ruled-out hypotheses — `react-arborist` ResizeObserver, focus/scroll-into-view, mobile-only, `transition-all duration-150` — `ChatTab.tsx` / `QueueTab.tsx` comparison confirming the bug is FileExplorer-local, two candidate fixes A/B, effort = **XS ~1 hr**, and 5 open questions for the user) lives at:
- **[`BACKLOG/specs/018-file-tree-scroll-on-parent-click.md`](BACKLOG/specs/018-file-tree-scroll-on-parent-click.md)**

→ Ready. Chosen fix: drop the static `md:block`; keep the existing responsive state branches. Regression, desktop overflow, and mobile panel-swap smoke gates are recorded in the spec.

### Spec 19 — Sibling-session listing + read tool for the management agent
**Status:** shipped · **Added:** 2026-07-14 · **Decisions resolved/rebased:** 2026-07-14 · **Merged/verified:** 2026-07-21 (in v0.7.6)

User said: *"we need to allow management agent to list other session-log in the same project. potentially for search or to grep something."*

**Resolved direction (from discussion, before spec):** **two new tools, not grep.** Grep can reach `orbital/sessions/*.jsonl` (it lives inside the workspace), but it's the wrong primitive — it's a content matcher, not a session browser:

- **Hard caps bite.** `grep_tool.py` enforces `_MAX_MATCHES=100`, `_MAX_COUNT_PER_FILE=50`, `_MAX_COLUMNS=500`, `_TIMEOUT_SECONDS=10`, and hitting any of them returns a synthetic error message — not partial results. This project alone has **23 sibling JSONLs**; one assistant-message line can be 5–10 KB, so the 500-char column cap silently truncates multi-tool-call turns.
- **No metadata access.** Sessions write `session_start` / `session_end` meta blocks (provider, model, sdk, origin, display name, timestamps, turn count, token totals). Grep sees text — "show me all sessions that used `claude-opus-4-20250514`" requires parsing JSONL inline.
- **No F1/F2 identity.** Format-1 `session_id` (user-facing chat id, persists across cold resumes) vs Format-2 `session_uuid` (filename stem — `{sanitized_project}_{uuid4().hex[:8]}`, per `session.py:167` and `project_paths.py:126-137`). Grep would have you grepping one and finding the other.
- **No ordering / pagination / filtering.** Sort by recency, top-N longest, "since yesterday" — none expressible in grep.

**Proposed:** two new tools,

1. `list_sessions(project=None, since=None, limit=20, sort="last_activity")` → metadata-only rows `{session_uuid, session_id (F1), name, origin, provider, model, started_at, last_activity_at, message_count, file_size}`. **Excludes the calling session** by default.
2. `read_session(session_uuid, from_line=None, max_lines=200, grep=None)` → streams messages from one chosen session, optionally substring-filtered. Composes with the existing `grep` for arbitrary content search inside a chosen session.

Two-tier design — listing is metadata-only (cheap, fast), reading is content (heavier, opt-in). Mirrors the existing two-step pattern in `_inject_recovery_context` (`context.py:94-186`: glób dir → sort by mtime → load last 20 messages), which is the closest prior art but is private, automatic, and reads only the single most recent session for cold resume.

The detailed spec (full code-path citations — `project_paths.py:126-137` (session file layout), `session.py:112-134` (F1/F2 docstring), `session.py:25-27` (ISO-8601 timestamps), `session.py:354-366` (regular message row format), `session.py:398-418` (first `_write_line` flush), `session.py:487-488` (model-swap comment), `session.py:508-548` (`sub_agent_thread`), `session.py:558-567` (`set_name` pattern), `session.py:581-611` (`session_start` meta rewrite), `session.py:33-55` (`_derive_name`), `context.py:78,96` (`_recovery_injected`), `context.py:108-109` (Layer-1 missing check), `context.py:116-117` (F2 comment), `context.py:128,133-135` (mtime sort), `context.py:137-147` (read-every-line), `context.py:153` (last 20 messages), `context.py:172` (tool/system role skip), `agent_manager.py:721` (tool_names → prompt), `agent_manager.py:1057-1197` (`_register_tools`), `agent_manager.py:1084-1108` (tool registration shape), `agent_manager.py:664-667` (F1 session_id threading), `grep_tool.py:36-39,120,222,228,232-234,288-289,300` (constants and output shape), `project_paths.py:143-160` (`sub_agents_dir` — separate, must NOT be listed), on-the-fly JSONL parse of `session_start`/`session_end` blocks (no sidecar index needed; 24 files is sub-ms), pagination/cap design mirroring grep's, sub-agent exclusion (sub-agents have their own session_uuid; including them would let mgmt agent read another agent's transcript — confirm intended vs unintended), **and 7+ open questions** for the user: archived/compressed sessions (if any), raw JSONL lines vs parsed message objects, scope — management-only or also sub-agents, cross-project isolation (`project=None` default = current project; what is "current project" for the mgmt agent?), read-only vs write-into (user's stated need is list/search/grep → read-only sufficient; write is a heavier story), PII redaction (sibling sessions can contain pasted API keys / personal data — does the existing PII handling in `agent_os/agent/` cover this?), and recovery-injection unification (now mgmt has two paths to a prior session — auto-injection + the new tool; refactor the auto path to call the new tool, or keep duplicated)) lives at:

- **[`BACKLOG/specs/019-list-sibling-sessions-tool.md`](BACKLOG/specs/019-list-sibling-sessions-tool.md)**

→ Ready after rebasing onto the current canonical-session implementation. Reuse the daemon's existing disk-backed session listing; current-project, management-only, read-only, parsed messages, self excluded by default, workers/sub-agents excluded, same-workspace trust, and no cold-resume refactor. Effort = **S** (~½–1 day).

### Spec 21 — Project-level sub-agent deployment instructions
**Status:** shipped · **Added:** 2026-07-16 · **Decisions:** normally-binding instructions + next-management-call application · **Merged/verified:** 2026-07-21 (in v0.7.6)

User said: *"we should expose a field for user to optionally tell the management agent how to deploy sub agents"* and confirmed that the current completion/error recovery loop is sufficient active supervision.

Implemented as one optional `sub_agent_deployment_instructions` textarea above the per-agent cards; project-JSON persistence; management-prompt-only injection; chat/queue/trigger parity; an empty-value no-op; and a contained live-value provider for existing sessions. The UI is localized in English and Simplified Chinese. No worker scoring, routing engine, or active-supervision behavior was added. Automated suites plus isolated real-daemon, same-session refresh, clear, queue-path, desktop, and 375 px mobile smoke passed.

Spec: **[`BACKLOG/specs/021-sub-agent-deployment-instructions.md`](BACKLOG/specs/021-sub-agent-deployment-instructions.md)**

→ Awaiting human diff review/merge. The user accepted the recommended decisions: instructions are normally binding subject to availability/safety/autonomy, and saved changes apply on the next management-model call, never an in-flight request.

### Spec 22 — Reactive sub-agent supervision and recovery contract
**Status:** shaped · **Added:** 2026-07-16 · **Alignment pending:** user-stop boundary + Check-in re-dispatch + retry guard

User said the management agent already recovers after bad sub-agent news by
finishing the work itself or restarting the worker, and rejected a separate
continuous-supervision design as overengineered. Code investigation confirms
that assessment: all honest terminal outcomes already inject into and wake the
owning management session, dispatch errors remain in-turn, successful output is
already subject to verification, and recovery tool calls already pass through
the management agent's autonomy interceptor.

The remaining gap is a small management-prompt contract for negative outcomes:
diagnose before retrying; choose direct correction, targeted same-thread
follow-up, deliberate fresh reset, alternate worker, or user escalation; never
blindly repeat; and treat an explicit user stop as a separate safety boundary.
The spec also records the non-obvious current policy that Check-in permits
automatic `agent_message(send)` while Supervised approval-gates it, and that
per-run send/ping-pong guards reset after each terminal wake rather than forming
a cross-retry cap. Recommended effort is **XS, ~0.5–1 day**, including focused
tests and precise README positioning; no watchdog, progress classifier,
automatic retry engine, or new UI.

Spec: **[`BACKLOG/specs/022-reactive-sub-agent-supervision.md`](BACKLOG/specs/022-reactive-sub-agent-supervision.md)**

→ Awaiting three policy confirmations: **user stop halts the stopped work across
all recovery routes** (recommended), **Check-in keeps automatic re-dispatch while
Supervised approval-gates it** (recommended), and **prompt-only no-blind-retry
guard rather than persistent retry counters** (recommended).
